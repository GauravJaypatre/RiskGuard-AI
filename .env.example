# RiskGuard AI — Environment Variables
# Copy this file to .env and fill in your values.

# Gemini API key for the investigation agent.
# If absent, the agent falls back to mock mode (visibly indicated).
GEMINI_API_KEY=
